enum ENV_MODE {
  PRODUCTION = 'production',
  DEVELOPMENT = 'development',
}

const FILE_UPLOADS_DIR = 'uploads';
const TEMP_UPLOADS_DIR = 'static/';
const ROUTE_PREFIX = '/api/v1';
export { ENV_MODE, ROUTE_PREFIX, FILE_UPLOADS_DIR, TEMP_UPLOADS_DIR };
